// import axios from "axios";

// type Props = {
//   username: string;
//   email: string;
//   password: string;
// };

// export const welcomeUser = async ({ username, password }: Props) => {
//   try {
//     const result = await axios.get(
//       `${import.meta.env.VITE_BACKEND_URL}welcome/${username}`
//     );

//     console.log(result);
//   } catch (error) {
//     console.error("Error registering user:", error);
//     throw error;
//   }
// };
